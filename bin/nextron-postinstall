#!/usr/bin/env node

const { npxSync } = require('node-npx');

const electronBuilder = `./node_modules/nextron/node_modules/.bin/electron-builder${process.platform === 'win32' ? '.cmd' : ''}`;

npxSync(electronBuilder, ['install-app-deps'], {
  cwd: process.cwd(),
  stdio: 'inherit',
});
